# People

`contact` (`cnt-`) nodes - people and orgs the work involves. Link them with typed edges
(e.g. a `decision` `made_by` a contact, a `project` `involves` one). Keep PII minimal; this is a
knowledge graph, not a CRM.
