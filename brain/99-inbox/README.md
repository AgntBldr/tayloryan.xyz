# Inbox

Drop unclassified thoughts here as `note` nodes (`not-` prefix, `status: raw`). Notes are
**temporary** - `/organize-vault` (Phase 2) graduates each into a proper typed node
(see `edge-cases.md`) and archives the note. Don't let notes pile up here.
