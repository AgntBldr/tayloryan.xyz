# Projects

One folder per project: `02-projects/<project>/`. A `project` (`prj-`) is `part_of` its
business pillar and usually `derived_from` a `playbook`; it contains its `task`, `decision`,
and `event` nodes. Created via `/start-project` (Phase 3).
