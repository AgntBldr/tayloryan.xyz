# secondbrain-template

Content-free engine for the V3 Second Brain. **Clone this per business / large project.**

Do not add business content here. To create a new vault, run `new-brain.ps1 -Name "<business>"`
from the workspace root (or use the `/new-brain` skill). See `ARCHITECTURE.md` + `00-system/AGENTS.md`.
