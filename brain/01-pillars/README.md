# Pillars

One `pillar` (`pil-`) per business or large domain - the root that everything hangs off. A `project`
is `part_of` its pillar; knowledge nodes scope to one with `tagged_with: ["[[pil-<business>]]"]`.
The vault's root pillar is created automatically when the vault is stamped (`/new-brain`).
