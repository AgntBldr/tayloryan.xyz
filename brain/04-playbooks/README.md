# Playbooks

Reusable SOPs and procedures as `playbook` (`pbk-`) nodes. A `project` is usually `derived_from` a
playbook; close-out learnings from finished projects flow back here (`/close-project`). A mature,
frequently-run playbook can be compiled into an executable skill via `/skillify` (Phase 5).
